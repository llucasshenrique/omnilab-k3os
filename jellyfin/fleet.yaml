namespace: jellyfin
helm:
  values:
    ingressRoute:
      enabled: true
      entryPoints:
        - websecure
      routes:
      - match: Host(`video.llucasshenrique.space`)
      tls:
        provider: cloudflare