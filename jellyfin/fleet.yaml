namespace: jellyfin
helm:
  values:
    ingressRoute:
      enabled: true
      entryPoints:
        - websecure
      route:
      - match: Host(`video.llucasshenrique.space`)
      tls:
        provider: cloudflare